{
  "name": "chinook-profile",
  "version": "0.1.0",
  "private": true,
  "description": "Chinook profile: minimal DSH runtime (Agent, Agent Loop, Session, Session Persistence, System Prompt, LLM, Tool Runtime) plus the chinook-dsh-plugin bundle.",
  "dsh": {
    "profile": {
      "bundles": [
        "@deepseek-ai/dsh-base",
        "chinook-dsh-plugin"
      ],
      "patchReload": "startup"
    }
  }
}
