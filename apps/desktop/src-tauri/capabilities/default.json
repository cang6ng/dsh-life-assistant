{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window of the DSH Chinook desktop host. Only the bundled sidecar is reachable — no arbitrary shell/command execution is exposed to the WebView (contract §46).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
