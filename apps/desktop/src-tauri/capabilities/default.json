{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window of the DSH Life Assistant desktop host. Only the bundled sidecar is reachable — no arbitrary shell/command execution is exposed to the WebView.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
